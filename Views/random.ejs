<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>random</title>
</head>
<body>
    <%= a %>
    <%= b %>
    <%= c %>
    <% for(let i of arr){%>
        <h2><%= i %></h2>
        <% } %>

    <% if(a%2===0){ %>
        <h1>Even </h1>
        <% } else { %>
        <h2>Odd</h2>   
        <% } %>
 

</body>
</html>